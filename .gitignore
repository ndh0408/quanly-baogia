node_modules/
.env
.env.local
.admin-credentials.local

# Local font copies (license-restricted; production deploy provides them via Dockerfile)
fonts/*.ttf
fonts/*.otf
!fonts/.keep
dist/
*.log
npm-debug.log*

# DB volume
postgres-data/

# Generated test artifacts
test_*.xlsx
test_*.pdf
gdpr.json
*.tmp.xlsx
temp_*.xlsx
_*.xlsx
_*.mjs
c.txt
cookies.txt
tok.txt
rtok.txt
dump*.txt
inspect_xls.py
Marico_GN2505.xls
m.xlsx

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp

# Untracked junk (audit): old code backups + one-off recon scripts
backups/

# Security scan artifacts (tools, PG binaries, reports — confidential, off-repo)
.scan/

# Internal audit reports — confidential, store off-repo (never commit)
AUDIT_REPORT.md
.audit-full.txt
audit*.md
AUDIT_*.txt

# UI/e2e test harnesses — contain dev creds, keep local only
e2e-ui.mjs
e2e*.mjs
*playwright*.mjs

# Scratch / planning notes — keep in Claude memory or internal wiki
.spa-plan.md
.*-plan.md

# Frontend mới (React/Vite) — build artifact + deps (off-repo, build trong Docker)
/public/app2/
web/node_modules/

# e2e screenshot outputs (test artifacts)
cmp-*.png
cust-*.png
customers-*.png
v-*.png
s2-*.png
spa-smoke.png

/inv-v2.png
/e2e-invoice-v2.mjs
/e2e-verify-ui.mjs
/ui-shots-after/
/e2e-freeze.mjs
/inv-freeze.png
/e2e-contract.mjs
/e2e-birthdate.mjs
/e2e-modal-shot.mjs
/modal-emp.png
/modal-add.png

/e2e-gridxl.mjs
/e2e-probe-shift.mjs
/e2e-probe-cells.mjs
/gridxl.png
/e2e-grid-excel2.mjs
/e2e-venues-page.mjs
/e2e-import-excel.mjs
/e2e-import-shot.mjs
/tmp-e2e-import.xlsx
/import-modal-*.png

# Trạng thái của AGENT / trình soạn thảo — có thể chứa lịch sử shell kèm thông tin đăng nhập,
# và .claude/ nặng ~18MB (89 skill BMAD) nên clone về vô ích. CLAUDE.md thì VẪN commit
# (hướng dẫn chung cho repo). Dạng KHÔNG có "/" đầu để bắt cả thư mục con.
.claude/
.agents/
_bmad/
_bmad-output/
.qwen/
ui-rc-shots/

# Ảnh chụp Redis do redis-server sinh ra khi chạy test ở thư mục repo (redis-cli SAVE/BGSAVE).
# Nó chứa dữ liệu phiên + job hàng đợi của lượt chạy — không phải mã nguồn, và không được lên Git.
dump.rdb
*.rdb

# Ảnh màn hình khi scripts/ci/ui-smoke.mjs đỏ — vật phẩm gỡ lỗi, không phải mã nguồn.
.ui-smoke/

# SBOM sinh ra bởi scripts/ci/security-scan.sh — dựng lại được từ package-lock.json,
# không phải mã nguồn. Bản đi kèm mỗi lần phát hành thì lưu ở nơi phát hành, không lưu ở đây.
sbom.cdx.json
