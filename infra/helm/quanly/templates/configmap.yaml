apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quanly.fullname" . }}-config
  labels: {{- include "quanly.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
