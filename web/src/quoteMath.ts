// Toán tiền + định dạng báo giá nay sống ở NGUỒN DUY NHẤT `shared/quote-math.ts` (dùng chung
// BE↔FE). File này GIỮ LẠI làm cầu re-export để mọi import `from "./quoteMath"` cũ không phải đổi.
export * from "../../shared/quote-math";
