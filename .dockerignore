node_modules
npm-debug.log
.git
.github
.vscode
.idea
.env
.env.local
.admin-credentials.local
test_*.xlsx
temp_*.xlsx
c.txt
cookies.txt
dump*.txt
*.md
.DS_Store
Thumbs.db
postgres-data

# Backups / archives / dumps — never inflate the image
backups/
*.bak
*.dump
*.tgz
*.tar
*.tar.gz
*.zip
prod-backup*

# Recon scripts & audit junk
.audit-full.txt
e2e*.mjs
*playwright*.mjs

# Logs & editor/CI cruft
*.log
.editorconfig
.gitattributes

# dist/ được BUILD BÊN TRONG image (tầng `build` của Dockerfile). Nếu để lọt bản dist local vào
# build context thì `COPY --from=build` vẫn thắng, nhưng context phình ra vô ích và dễ gây nhầm
# tưởng là image dùng bản biên dịch trên máy dev.
dist/
coverage/
_bmad-output/
